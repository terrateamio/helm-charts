{{- include "terrateam.validateConfig" . -}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "application.name" $ }}-{{ .Values.terrateam.name }}
  namespace: {{ template "application.namespace" $ }}
  {{- with (mergeOverwrite (deepCopy .Values.global.annotations) .Values.terrateam.annotations) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "terrateam.labels" $ | nindent 4 }}
    {{- with (mergeOverwrite (deepCopy .Values.global.labels) .Values.terrateam.labels) }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if not .Values.terrateam.autoscaler.enabled }}
  replicas: {{ .Values.terrateam.replicaCount }}
  {{- end }}
  revisionHistoryLimit: {{ .Values.terrateam.revisionHistoryLimit }}
  {{- with .Values.terrateam.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "terrateam.selectorLabels" $ | nindent 6 }}
  template:
    metadata:
      {{- $podAnnotations := mergeOverwrite (deepCopy .Values.global.annotations) .Values.terrateam.annotations }}
      {{- with (include "terrateam.managedSecrets" . | fromYaml) }}
      {{- $_ := set $podAnnotations "checksum/secrets" (toYaml . | sha256sum) }}
      {{- end }}
      {{- with $podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "terrateam.labels" $ | nindent 8 }}
        {{- with (mergeOverwrite (deepCopy .Values.global.labels) .Values.terrateam.labels) }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with (mergeOverwrite .Values.global.nodeSelector .Values.terrateam.nodeSelector) }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (coalesce .Values.terrateam.tolerations .Values.global.tolerations) }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (mergeOverwrite .Values.global.affinity .Values.terrateam.affinity) }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (mergeOverwrite .Values.global.securityContext .Values.terrateam.securityContext) }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (coalesce .Values.terrateam.imagePullSecrets .Values.global.imagePullSecrets) }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.rbac.serviceAccount.enabled }}
      {{- if .Values.rbac.serviceAccount.name }}
      serviceAccountName: {{ .Values.rbac.serviceAccount.name }}
      {{- else }}
      serviceAccountName: {{ template "application.name" $ }}
      {{- end }}
      {{- end }}
      {{- if .Values.terrateam.waitForDb.enabled }}
      initContainers:
        - name: wait-for-db
          image: {{ printf "%s:%s" .Values.terrateam.waitForDb.image.repository .Values.terrateam.waitForDb.image.tag }}
          imagePullPolicy: {{ .Values.terrateam.waitForDb.image.pullPolicy }}
          {{- with .Values.terrateam.waitForDb.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: PGHOST
              value: {{ include "terrateam.dbHost" . | quote }}
            - name: PGPORT
              value: {{ .Values.terrateam.config.db.port | default 5432 | quote }}
            - name: PGUSER
              value: {{ .Values.terrateam.config.db.username | quote }}
            - name: PGDATABASE
              value: {{ .Values.terrateam.config.db.databaseName | quote }}
          command:
            - sh
            - -c
            - |
              deadline=$(( $(date +%s) + {{ .Values.terrateam.waitForDb.timeoutSeconds }} ))
              until pg_isready --quiet; do
                if [ "$(date +%s)" -ge "$deadline" ]; then
                  echo "timed out after {{ .Values.terrateam.waitForDb.timeoutSeconds }}s waiting for PostgreSQL at $PGHOST:$PGPORT"
                  exit 1
                fi
                echo "waiting for PostgreSQL at $PGHOST:$PGPORT"
                sleep 2
              done
              echo "PostgreSQL at $PGHOST:$PGPORT is accepting connections"
      {{- end }}
      containers:
        - name: {{ template "application.name" $ }}-{{ .Values.terrateam.name }}
          image: {{ printf "%s:%s" .Values.terrateam.image.repository .Values.terrateam.image.tag }}
          imagePullPolicy: {{ .Values.terrateam.image.pullPolicy | default "Always" }}
          {{- with .Values.terrateam.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          {{- if .Values.terrateam.livenessProbe.enabled }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            failureThreshold: {{ .Values.terrateam.livenessProbe.failureThreshold }}
            initialDelaySeconds: {{ .Values.terrateam.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.terrateam.livenessProbe.periodSeconds }}
            successThreshold: {{ .Values.terrateam.livenessProbe.successThreshold }}
            timeoutSeconds: {{ .Values.terrateam.livenessProbe.timeoutSeconds }}
          {{- end }}
          {{- if .Values.terrateam.readinessProbe.enabled }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            failureThreshold: {{ .Values.terrateam.readinessProbe.failureThreshold }}
            initialDelaySeconds: {{ .Values.terrateam.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.terrateam.readinessProbe.periodSeconds }}
            successThreshold: {{ .Values.terrateam.readinessProbe.successThreshold }}
            timeoutSeconds: {{ .Values.terrateam.readinessProbe.timeoutSeconds }}
          {{- end }}
          env:
            - name: "TERRAT_API_BASE"
              value: {{ .Values.terrateam.config.apiEndpoint | default (printf "https://%s/api" .Values.terrateam.config.fqdn) | quote }}
            - name: "TERRAT_WEB_BASE_URL"
              value: {{ .Values.terrateam.config.webBaseUrl | default (printf "https://%s" .Values.terrateam.config.fqdn) | quote }}
            - name: "TERRAT_UI_BASE"
              value: {{ .Values.terrateam.config.uiBase | default (printf "https://%s" .Values.terrateam.config.fqdn) | quote }}
            - name: "TERRAT_TELEMETRY_LEVEL"
              value: {{ .Values.terrateam.config.telemetryLevel | quote }}

            - name: DB_HOST
              value: {{ include "terrateam.dbHost" . | quote }}
            - name: DB_PORT
              value: {{ .Values.terrateam.config.db.port | default 5432 | quote }}
            - name: DB_USER
              value: {{ .Values.terrateam.config.db.username | quote }}
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.db.passwordSecretName }}
                  key: {{ .Values.terrateam.config.db.passwordSecretKey }}
            - name: DB_NAME
              value: {{ .Values.terrateam.config.db.databaseName | quote }}

            {{ if .Values.terrateam.config.github.enabled -}}
            - name: GITHUB_APP_ID
              valueFrom: # TODO: This doesn't have to be a secret
                secretKeyRef:
                  name: {{ .Values.terrateam.config.github.appIdSecretName }}
                  key: {{ .Values.terrateam.config.github.appIdSecretKey }}
            - name: GITHUB_APP_CLIENT_ID # TODO: This doesn't have to be a secret
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.github.appClientIdSecretName }}
                  key: {{ .Values.terrateam.config.github.appClientIdSecretKey }}
            - name: GITHUB_APP_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.github.appClientSecretSecretName }}
                  key: {{ .Values.terrateam.config.github.appClientSecretSecretKey }}
            - name: GITHUB_APP_PEM
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.github.appPrivatePemCertificateSecretName }}
                  key: {{ .Values.terrateam.config.github.appPrivatePemCertificateSecretKey }}
            - name: GITHUB_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.github.webhookSecretName }}
                  key: {{ .Values.terrateam.config.github.webhookSecretKey }}
            {{- if .Values.terrateam.config.github.appUrl }}
            - name: GITHUB_APP_URL
              value: {{ .Values.terrateam.config.github.appUrl | quote }}
            {{- end }}
            {{- if .Values.terrateam.config.github.apiBaseUrl }}
            - name: GITHUB_API_BASE_URL
              value: {{ .Values.terrateam.config.github.apiBaseUrl | quote }}
            {{- end }}
            {{- if .Values.terrateam.config.github.webBaseUrl }}
            - name: GITHUB_WEB_BASE_URL
              value: {{ .Values.terrateam.config.github.webBaseUrl | quote }}
            {{- end }}
            {{- end }}

            {{- if .Values.terrateam.config.gitlab.enabled -}}
            - name: GITLAB_APP_ID
              valueFrom: # TODO: This doesn't have to be a secret
                secretKeyRef:
                  name: {{ .Values.terrateam.config.gitlab.appIdSecretName }}
                  key: {{ .Values.terrateam.config.gitlab.appIdSecretKey }}
            - name: GITLAB_APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.gitlab.appSecretSecretName }}
                  key: {{ .Values.terrateam.config.gitlab.appSecretSecretKey }}
            - name: GITLAB_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.gitlab.accessTokenSecretName }}
                  key: {{ .Values.terrateam.config.gitlab.accessTokenSecretKey }}
            {{- if .Values.terrateam.config.gitlab.apiBaseUrl }}
            - name: GITLAB_API_BASE_URL
              value: {{ .Values.terrateam.config.gitlab.apiBaseUrl | quote }}
            {{- end }}
            {{- if .Values.terrateam.config.gitlab.webBaseUrl }}
            - name: GITLAB_WEB_BASE_URL
              value: {{ .Values.terrateam.config.gitlab.webBaseUrl | quote }}
            {{- end }}
            {{- end }}

            {{- if .Values.terrateam.config.infracost.selfHostedApiKeySecretName }}
            - name: SELF_HOSTED_INFRACOST_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.terrateam.config.infracost.selfHostedApiKeySecretName }}
                  key: {{ .Values.terrateam.config.infracost.selfHostedApiKeySecretKey }}
            {{- else }}
            - name: SELF_HOSTED_INFRACOST_API_KEY
              value: {{ .Values.terrateam.config.infracost.selfHostedApiKey | quote }}
            {{- end }}
            - name: "INFRACOST_PRICING_API_ENDPOINT"
              value: {{ .Values.terrateam.config.infracost.pricingApiEndpoint | quote }}

            {{- if .Values.terrateam.config.extraEnv }}
            {{- toYaml .Values.terrateam.config.extraEnv | nindent 12 }}
            {{- end }}
